{
  "name": "api",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/api",
  "targets": {
    "build": {
      "options": {
        "main": "./cmd/api"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{projectRoot}",
        "commands": [
          "gow run ./cmd/api"
        ],
        "parallel": false
      }
    },
    "test": {
      "options": {
        "cover": true
      }
    }
  },
  "tags": []
}
