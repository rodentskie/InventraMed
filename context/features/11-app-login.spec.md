### App Login Page

This will be on `apps/app`.
Login page

## Requirements

I wired up [chakra](https://chakra-ui.com/) UI in the app already.
Snippets are all added in `libs/ts/snippets/src/lib`.

Sample usage of components are in `apps/app/src/app/page.tsx`.
Attached is a screenshot of the prototype, look into the login page only.

Prototype has reference style from notion and github.

## References

- @apps/app/src/app/page.tsx
- @context/screenshots/InventraMed Prototype.pdf
- @apps/api/internal/handler/swagger/openapi.json